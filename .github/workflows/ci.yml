---
name: ci

on:
  schedule:
    - cron: "0 4 * * 0"
  workflow_dispatch:
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - master
      - unicorn
    tags:
      - 'v*'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Get latest release
        id: kaniko
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: GoogleContainerTools/kaniko

      - name: Get latest release
        id: jq
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: stedolan/jq

      - name: Get latest release
        id: yq
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: mikefarah/yq

      - name: Get latest release
        id: pushrm
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: christian-korneck/docker-pushrm

      - name: Checkout
        uses: actions/checkout@v3

      - name: PrepareReg Names
        run: |
          echo IMAGE_REPOSITORY=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]') >> $GITHUB_ENV
          echo IMAGE_TAG=$(echo ${{ github.ref }} | tr '[:upper:]' '[:lower:]' | awk '{split($0,a,"/"); print a[3]}') >> $GITHUB_ENV

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            ${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: set up qemu
        uses: docker/setup-qemu-action@v2

      - name: set up docker buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          platforms: linux/amd64,linux/arm64
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          #tags: ${{ steps.meta.outputs.tags }}, ${{ env.IMAGE_REPOSITORY }}:${{ steps.kaniko.outputs.release }}, ${{ env.IMAGE_REPOSITORY }}:latest
          tags: latest
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            KANIKO_RELEASE=${{ steps.kaniko.outputs.release }}-debug
            JQ_RELEASE==${{ steps.jq.outputs.release }}
            YQ_RELEASE==${{ steps.yq.outputs.release }}
            PUSHRM_RELEASE==${{ steps.pushrm.outputs.release }}
